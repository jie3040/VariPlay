# VeriPlay Tests

Run from the repository root:

```bash
pytest tests/
```

The focused VeriPlay test suite currently contains 27 tests covering parser, verifier execution, perturbation, critic scoring, critic reward and critic train-data construction.
